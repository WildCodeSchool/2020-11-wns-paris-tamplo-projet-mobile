import React from "react";
import { ScrollView, Text } from "react-native";
import Container from "../templates/Container";

const Home = () => {
  return (
    <Container>
      <ScrollView>
        <Text>On est dans home</Text>
      </ScrollView>
    </Container>
  );
};

export default Home;
